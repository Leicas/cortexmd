/**
 * Public entry for the dashboard module. Re-exports `dashboardRouter` so
 * consumers can `import { dashboardRouter } from './dashboard/index.js'`.
 */
export { dashboardRouter } from './controller.js';
